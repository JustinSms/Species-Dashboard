[build]
buildCommand = "cd frontend && npm ci && npm run build"

[deploy]
startCommand = "cd backend && uv sync && uv run uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
